## What

<!-- Adding / updating / removing which entries? At most 3 new entries per PR. -->

## Checklist

- [ ] One YAML file per entry under `data/entries/`; READMEs regenerated with `npm run build`
- [ ] Description is factual and matches the code (numbers and command names checked)
- [ ] No secrets — only placeholders like `<API_KEY>`
- [ ] Not a duplicate: if an existing entry solves the same problem, this is added as an `alternative`
- [ ] `npm test` passes
- [ ] Tested on: <!-- clients -->
